{
  "mcpServers": {
    "notestorelab": {
      "command": "./bin/notestorelab-mcp",
      "env": {
        "NOTESTORELAB_REPO_ROOT": "${NOTESTORELAB_REPO_ROOT}",
        "NOTESTORELAB_CASE_DIR": "${NOTESTORELAB_CASE_DIR}",
        "NOTESTORELAB_PYTHON": "${NOTESTORELAB_PYTHON}"
      }
    }
  }
}
